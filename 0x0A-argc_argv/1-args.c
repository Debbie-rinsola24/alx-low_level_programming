#include <stdio.h>

/**
 * main - write a programme that  print the number of arguments passed into 
 * @argc: argument counter
 * @argv: argument vector
 *
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%d\n", args - 1);
	(void)argv;
	return (0);
}
